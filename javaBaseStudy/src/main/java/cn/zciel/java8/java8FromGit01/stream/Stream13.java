package cn.zciel.java8.java8FromGit01.stream;

import sun.security.provider.SecureRandom;

import java.lang.reflect.Array;
import java.util.Arrays;
import java.util.stream.IntStream;

/**
 * @author YINZHEN
 * @date 2019/3/22 14:35
 * @Description
 */
public class Stream13 {

    public static void main(String[] args) {
        /*SecureRandom secureRandom = new SecurityException(new byte[]{1, 3, 3, 7});
        int[] randoms = IntStream.generate(secureRandom::nextInt)
                .filter(n -> n > 0)
                .limit(10)
                .toArray();
        System.out.println(Arrays.toString(randoms));

        int[] nums = IntStream.iterate(1, n -> n * 2)
                .limit(11)
                .toArray();
        System.out.println(Arrays.toString(nums));*/
    }

}
